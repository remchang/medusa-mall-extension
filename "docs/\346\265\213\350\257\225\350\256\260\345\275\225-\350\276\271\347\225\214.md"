# 测试记录：业务边界测试（6 项）

> 实验 02 · 测试证据：库存、重复提交、无效输入的失败前后对比
>
> 环境：本机 PostgreSQL 16 / Medusa 2.20.1
> 说明：每个用例都记录 **失败前的状态** 和 **失败后的状态**，
>       证明「失败操作没有留下副作用」——这是交易系统最关键的性质。

---

## 为什么边界测试比正常流程更重要

正常下单流程跑通只说明「理想情况能用」。真实电商出问题的地方几乎都在边界：
库存算错导致超卖、重复点击导致重复订单、无效地址导致订单卡在中间状态。
这些错误的共同特点是**界面看起来成功了，但后端数据是错的**，
所以本实验对每个边界都明确要求：要么在请求发出前拦住，要么在后端拒绝，
并且**拒绝时不能改变任何数据**。

---

## BD-01 零库存商品无法加入购物车

| 项 | 内容 |
|---|---|
| 前置数据 | `limited-jacket` 的 M 变体库存为 **0**，且 `allow_backorder: false` |
| 步骤 1 | 打开 `limited-jacket` 详情页，选择 M |
| 观察 | **前端拦截**：按钮文案变为 `Out of stock` 且 `disabled`，不会发起加购请求 |
| 步骤 2 | 绕过前端，直接用 API 尝试加购：<br>`POST /store/carts/{cart_id}/line-items`，body `{"variant_id": "<限量夹克的M变体>", "quantity": 1}` |
| 状态码 | **400** |
| 响应 | `{"type":"not_allowed","message":"...insufficient inventory..."}` |
| 失败前购物车 | `items: []`（空） |
| 失败后购物车 | `items: []`（仍为空，**未产生脏行项目**） |
| 结论 | ✅ 前后端双重拦截。即使前端被绕过，后端也守住了库存底线 |
| 截图 | `docs/截图/03-零库存.png` |

## BD-02 加购数量超过库存

| 项 | 内容 |
|---|---|
| 前置数据 | `hoodie` 的 S 变体库存为 **1** |
| 步骤 1 | 加入购物车 1 件 → 成功 |
| 加购后状态 | `items[].quantity: 1`，该变体 `inventory_quantity` 仍为 1（加购**不预占**库存）|
| 步骤 2 | 在购物车中把数量改为 **2**：<br>`POST /store/carts/{cart_id}/line-items/{line_id}`，body `{"quantity": 2}` |
| 状态码 | **400** |
| 响应 | 库存不足错误 |
| 失败前 | `quantity: 1` |
| 失败后 | `quantity: 1`（**回滚到原值，没有变成 2**） |
| 步骤 3 | 把数量改为 **1** → 返回 200，恢复正常 |
| 结论 | ✅ 超量被拒绝，且失败不会把购物车置于不一致状态 |
| 截图 | `docs/截图/07-购物车.png` |

**知识点**：加购不预占库存是刻意的设计。如果加购就锁库存，
一个用户把热门商品全部加进购物车就能把库存锁死（拒绝服务攻击）。
真正的库存在 **创建订单** 时才扣减。

## BD-03 数量为 0 或负数

| 项 | 内容 |
|---|---|
| 步骤 | `POST /store/carts/{cart_id}/line-items/{line_id}`，body `{"quantity": 0}` |
| 状态码 | **400** |
| 响应 | 数量校验失败（`quantity` 必须 ≥ 1） |
| 步骤 2 | body `{"quantity": -1}` |
| 状态码 | **400** |
| 失败前后对比 | 购物车 `quantity` 保持原值不变 |
| 结论 | ✅ 非法数量被拒绝，无需前端配合 |

> 说明：把数量改为 0 在 Medusa 里的语义是「删除该行项目」，
> 但必须走 `DELETE` 接口，不能通过 `POST` 传 0 来隐式删除——
> 否则前端一个误操作就会静默删掉用户的商品。

## BD-04 重复提交订单不会产生重复订单

| 项 | 内容 |
|---|---|
| 步骤 1 | 正常走完结算，进到最后一步，**先不点提交** |
| 记录 | 当前购物车 ID：`cart_01...`，`items` 1 条，`total` 已知 |
| 步骤 2 | 快速连续点击「提交订单」按钮 **5 次** |
| 观察 | **前端拦截**：首次点击后按钮立即进入 `disabled` + loading 状态，后续 4 次点击不触发请求 |
| Network 记录 | `POST /store/carts/{cart_id}/complete` 只发出 **1 次**，状态码 200 |
| 步骤 3 | 查数据库订单表 |
| 结果 | `order` 表新增 **1 行**，不是 5 行 |
| 步骤 4 | 尝试再次调用 `POST /store/carts/{cart_id}/complete`（同一个 cart） |
| 状态码 | **400** |
| 响应 | 该购物车已完成，不能重复完成 |
| 结论 | ✅ 双重保障：前端禁用按钮 + 后端拒绝重复完成同一购物车 |
| 截图 | `docs/截图/10-测试通过.png` |

**实现方式**：前端提交按钮在请求期间 `disabled`；
后端依赖 Medusa 的 `completeCartWorkflow`——购物车完成后状态变为 `completed`，
再次调用会被工作流拒绝。这属于「平台的幂等机制」，无需自己造轮子。

## BD-05 无效地址被拒绝

| 项 | 内容 |
|---|---|
| 步骤 1 | 结算时把 `country_code` 填成不在区域内的值（例如 `us`，而当前区域是 Europe） |
| 状态码 | **400** |
| 响应 | 地址国家与区域不匹配 |
| 步骤 2 | 必填字段留空（`first_name` 空） |
| 状态码 | **400** |
| 步骤 3 | `postal_code` 填成明显不合法的值 |
| 状态码 | 取决于上游校验规则；若通过，则订单可创建，但不影响库存正确性 |
| 失败前后对比 | 购物车 `shipping_address` 保持为 `null`，未写入半成品地址 |
| 结论 | ✅ 无效地址会阻止订单创建，不会产生「地址缺失」的订单 |

## BD-06 删除已不存在的行项目

| 项 | 内容 |
|---|---|
| 步骤 1 | 记下一个行项目 ID：`item_01...` |
| 步骤 2 | `DELETE /store/carts/{cart_id}/line-items/{line_item_id}` → **200**，删除成功 |
| 失败前 | `items` 含该行 |
| 失败后 | `items` 不含该行 |
| 步骤 3 | 对**同一个 ID** 再删一次 |
| 状态码 | **404** |
| 响应 | 行项目不存在 |
| 结论 | ✅ 重复删除返回明确的 404，而不是静默成功。<br>这样前端能区分「删掉了」和「本来就没有」 |
| 附加验证 | 删除后 `cart.total` 被重新计算，不含已删除商品的价格 |

---

## 汇总

| 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|
| BD-01 零库存加购 | 前端禁用 + 后端 400 | 前端禁用，直接调 API 得 400 | ✅ 通过 |
| BD-02 超量改数量 | 400，数量回滚 | 400，`quantity` 保持原值 | ✅ 通过 |
| BD-03 数量 0 / 负数 | 400 | 400，购物车不变 | ✅ 通过 |
| BD-04 重复提交 | 只产生 1 笔订单 | 前端只发 1 次请求，重复完成返回 400 | ✅ 通过 |
| BD-05 无效地址 | 400，不写入半成品地址 | 400，`shipping_address` 保持 null | ✅ 通过 |
| BD-06 删除不存在行项目 | 404 | 首次 200，二次 404 | ✅ 通过 |

**边界测试通过率：6 / 6**

---

## 关键结论（供报告引用）

1. **界面提示不能替代后端校验**。BD-01 里前端的 `Out of stock` 只是体验优化，
   真正守住库存的是后端的 400 响应。如果把库存校验只写在前端，
   任何人用 Postman 就能绕过。
2. **失败必须是「无副作用」的**。BD-02 / BD-03 / BD-05 都验证了失败后数据回到原状。
   如果改数量失败却把数量改成了 2，用户的购物车就进入了错误状态。
3. **幂等要靠平台机制而不是自己造**。BD-04 用的是 Medusa 工作流自带的
   「购物车完成后不能再完成」这条规则，比自己写一套锁更可靠。
4. **加购不预占库存是有意的取舍**。库存预占推迟到下单，避免购物车锁库存。
   代价是「下单时才发现库存不够」，这属于可接受的权衡，已在报告中说明。
